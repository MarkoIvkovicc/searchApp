SELECT users.id, users.email, users.name, users.password, developertype.name, programminglanguage.name, framework.name, subframework.name FROM users INNER JOIN developertype ON (users.developerId = developertype.id) INNER JOIN programminglanguage ON (users.progLangId = programminglanguage.id) INNER JOIN framework on (users.frameworkId = framework.id) LEFT OUTER JOIN subframework on (users.subFrameworkId = subframework.id) WHERE (users.id = 1 )

//New Query
SELECT users.id, users.email, users.name, users.password, developertype.name, programminglanguage.name, framework.name, subframework.name
FROM users
INNER JOIN developertype ON (users.developerId = developertype.id)
INNER JOIN programminglanguage ON (users.progLangId = programminglanguage.id)
LEFT OUTER JOIN framework on (users.frameworkId = framework.id)
LEFT OUTER JOIN subframework on (users.subFrameworkId = subframework.id)
WHERE users.name LIKE '%NAME%' OR users.email LIKE '%EMAIL%'